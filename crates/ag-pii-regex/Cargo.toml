[package]
name    = "ag-pii-regex"
version = "0.1.0"
edition = "2021"
description = "Regex fast-path PII detection (zero-copy over &str)"

[dependencies]
ag-types = { path = "../ag-types" }
regex    = { workspace = true }
once_cell = { workspace = true }
